{
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/estree": "^1.0.8",
    "@types/json-schema": "^7.0.15",
    "@types/raf": "^3.4.3",
    "@types/trusted-types": "^2.0.7",
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "predev": "npm cache clean --force && echo '🧹 npm cache cleared!'",
    "dev": "concurrently \"npm run dev --prefix Backend\" \"npm run dev --prefix ayur-namaste-sync\""
  }
}
